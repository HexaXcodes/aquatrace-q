# --- Web framework ------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19

# --- Config / validation --------------------------------------------------
pydantic==2.10.3
pydantic-settings==2.6.1

# --- Database ---------------------------------------------------------------
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
GeoAlchemy2==0.15.2

# --- Image handling (Phase 5 sonar-image parser) -----------------------------
Pillow==11.0.0

# --- Scientific / ML (Phases 6-10) -------------------------------------------
numpy==2.5.2
scipy==1.18.1
scikit-learn==1.9.0
joblib==1.4.2

# --- E004 shipwreck segmentation (Phase 6 detector) --------------------------
# CPU-only wheel -- install with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# A plain `pip install -r requirements.txt` also works but may pull the
# much larger CUDA build depending on the resolver/platform.
#
# Pinned to 2.14.0, not 2.13.0: installing ultralytics (below) pulls in
# torch>=1.8.0 as its own dependency and the resolver picked 2.14.0+cpu,
# upgrading the previously-pinned 2.13.0+cpu in place (confirmed still a
# CPU build, not the much larger CUDA one). Re-pinning here so this file
# matches what a fresh `pip install -r requirements.txt` actually
# resolves to, instead of drifting silently. E004 was re-verified against
# this version (tests/test_e004_detector.py) before this pin was bumped.
torch==2.14.0

# --- YOLO11s marine debris/gear detection (Phase 6 second detector) ----------
ultralytics==8.4.129

# --- Quantum ML (Phase 9) -----------------------------------------------------
qiskit==1.3.1
qiskit-machine-learning==0.8.2
qiskit-algorithms==0.3.1

# --- Geospatial (Phases 11-13) ------------------------------------------------
shapely==2.1.2
pyproj==3.7.2

# --- Testing ------------------------------------------------------------------
pytest==8.3.4
httpx==0.28.1

# --- Dev/env ----------------------------------------------------------------
python-dotenv==1.0.1
